package ufc.victor.localenv;

import ufc.victor.protocol.coordinator.node.Node;
import ufc.victor.protocol.coordinator.node.NodeId;

public class LocalNode extends Node {
    public LocalNode(NodeId id) {
        super(id);
    }
}
